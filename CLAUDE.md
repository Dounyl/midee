## Architecture Rules

Read `AGENTS.md` for project architecture rules.

## Planning Documents

When creating planning documents or task plans:
- Place all planning files in the `plan/` directory at the repo root
- Use descriptive filenames with dates: `YYYY-MM-DD-feature-name.md`
